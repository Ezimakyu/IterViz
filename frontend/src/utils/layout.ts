/**
 * Oval/pill node dimensions used by the force simulation and floating
 * edges. React Flow still needs `width`/`height` hints to position
 * handles and compute intersections.
 */
export const NODE_WIDTH = 200;
export const NODE_HEIGHT = 72;
